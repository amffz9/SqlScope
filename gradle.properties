# Gradle performance
org.gradle.caching=true
# Configuration cache is disabled because the IntelliJ Platform plugin has
# some tasks that are not yet configuration-cache compatible.
org.gradle.configuration-cache=false

# Suppress the "kotlin.stdlib added to the compile classpath" warning.
# The stdlib is provided by the IntelliJ Platform runtime.
kotlin.stdlib.default.dependency=false
